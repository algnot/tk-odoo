permissions:
  contents: read
  packages: write

name: Build Workflow

on: [push]

jobs:

  Build:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: |
          docker build -t ghcr.io/algnot/tk-odoo:${{ github.sha }} .

      - name: Login to GHCR
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          docker push ghcr.io/algnot/tk-odoo:${{ github.sha }}

      - name: Tag latest
        run: |
          docker tag ghcr.io/algnot/tk-odoo:${{ github.sha }} ghcr.io/algnot/tk-odoo:latest
          docker push ghcr.io/algnot/tk-odoo:latest
